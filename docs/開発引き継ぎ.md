# 開発引き継ぎ（banshu-survive）

Cursor のチャットスレッドは、アプリを閉じたり環境によっては履歴が残らないことがあります。**このファイルは Git に含まれるため、スレッドが消えてもここから続きを再開できます。**

---

## リポジトリ・デプロイ

| 項目 | 内容 |
|------|------|
| リポジトリ | `100mensoul/banshu-survive` |
| 直近の main コミット（記載時点） | `c61e3d7` — あらすじの節目ブロック矢印・screenful まわり |
| デプロイ | `git push origin main` で GitHub へ（GitHub Pages 利用時は反映に数十秒〜数分） |

ローカル作業ディレクトリ例: `…/100mensoul-search/banshu-survive`

---

## 直近までにやったこと（要約）

### あらすじページ `html/arasuji.html` + `css/arasuji.css`

1. **フラグメント（`#`）ジャンプと flex 縦中央のずれ対策**  
   - `.arasuji-narrative` の `padding-top` をやめ、セクションは **`margin-top`**、各 **`arasuji-narrative__screenful`** に `padding-top` を移した。  
   - **`100svh` は削除**し、`100vh` → `100dvh` のみ。  
   - アンカー用の **`id` は「外側の screenful」** に付与（`#arasuji-narrative-part1` / `#arasuji-narrative-part3`）。  
   - `#arasuji-narrative-part1` と `#arasuji-narrative-part3` は **`scroll-margin-top: 0`**。part2 / part4 は `0.85rem`。

2. **1画面目（故郷〜原風景）**  
   - `screenful--first` + `first-stack` + chunk×2 + **単一↓**（`arasuji-scroll-hint__stack--single`）。  
   - 内側クラス名は **`arasuji-narrative__first-stack`**（`first-center` などと混同しないこと）。

3. **2画面目（節目〜道の駅）**  
   - **`arasuji-narrative__screenful--centered`** を追加（`--first` と同じ縦中央・`padding-top: 0`）。  
   - 構造は 1画面目と同型：`screenful#part3` → `first-stack` → chunk×2 → 次章への矢印。

4. **矢印**  
   - 節目ブロック → part4 の矢印を **二重シェブロンから単一**に変更。  
   - `arasuji.css` のクエリは更新のたびに **`?v=` をインクリメント**（キャッシュ対策）。**記載時点: `?v=43`**。

### その他アセット

- 桜: `css/sakura-petals.css` / `js/sakura-petals.js`（ヒーローパネル内 `#arasuji-sakura`）。

---

## 主要ファイル一覧

| パス | 役割 |
|------|------|
| `html/arasuji.html` | あらすじ本文・screenful 構造・スクロールヒント |
| `css/arasuji.css` | レイアウト・ヒーロー・本文・フッター帯 |
| `css/main.css` | 共通 |
| `css/footer.css` | フッター（グレー帯） |
| `css/sakura-petals.css` | 桜の演出 |
| `js/arasuji-float-lines.js` | ログライン等 |
| `js/arasuji-narrative-fade.js` | 本文フェード |
| `js/sakura-petals.js` | 桜 |

---

## 次回やる候補（未着手・任意）

- **ファーストビュー「本文へ」矢印**は、現状まだ **二重シェブロン**の可能性あり。本文と揃えて単一にするかは好みで判断。  
- **アンカー + `scrollIntoView({ block: 'start' | 'center', behavior: 'smooth' })`** の JS を足すと、モバイル Safari 等での位置のブレをさらに抑えられる（CSS だけの限界の補助）。  
- **part4「今しかない…」** を、必要なら `screenful` + `centered` で他ブロックと揃える。  
- Cursor スレッドが消えたとき: **このファイルを開き**、「`docs/開発引き継ぎ.md` に沿って続きから」と新しいチャットに書くと復帰しやすい。

---

## 更新ルール

- あらすじの見た目や CSS を直したら **`html/arasuji.html` の `arasuji.css?v=` を上げる**。  
- 意味のあるまとまりでコミットし、`main` に push すれば本番と同期。

---

*最終更新: 2026-04-12（スレッド消失対策として作成）*
